%function [F1_new, F2_new]=fps_sorting(F1,F2)

for i=1:10%length(F1(:,1))
    
    val=abs(F1(i,1)-F2(i,1));
    
    if val<90
    F1_new(i,1:3)=F1(i,1:3);
    F2_new(i,1:3)=F2(i,1:3);    
    else 
    
        
    end
    
    
    
end %end of for loop

%end %end of function